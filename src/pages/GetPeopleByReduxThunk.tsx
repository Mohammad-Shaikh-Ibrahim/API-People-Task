// pages/GetPeopleByThunk.tsx
// import { useEffect } from 'react';
// import { useDispatch, useSelector } from 'react-redux';
// import type { RootState, AppDispatch } from '../app/store';
// import { fetchPeopleThunk } from '../features/peopleSlice';

const GetPeopleByReduxThunk: React.FC = () => {
//   const dispatch = useDispatch<AppDispatch>();
//   const { people, loading, error } = useSelector((state: RootState) => state.people);

//   useEffect(() => {
//     dispatch(fetchPeopleThunk());
//   }, [dispatch]);

//   if (loading) return <p>Loading...</p>;
//   if (error) return <p>Error: {error}</p>;

//   return (
//     <div>
//       <h1>People List</h1>
//       <ul>
//         {people.map((person, index) => (
//           <li key={index}>{person.name}</li>
//         ))}
//       </ul>
//     </div>
//   );
};

export default GetPeopleByReduxThunk;
